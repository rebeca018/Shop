/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "img.freepik.com",
            "upload.wikimedia.org",
            "img.icons8.com"
        ]
    }
};

export default nextConfig;
