/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "fakestoreapi.com",
            "img.icons8.com"
        ]
    }
};

export default nextConfig;
